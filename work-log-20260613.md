# 项目开发记录 — 2026-06-13

## 本次完成的代码改动

### 1. ✅ Sentinel 服务容错（已完成）
- `order-service/src/main/java/.../config/SentinelRuleConfig.java` — **新增**：流控规则（createOrder 10QPS、listOrders 20QPS、cancelOrder 5QPS）+ 熔断规则（慢调用 RT>2000ms 比例≥50% 熔断 10s）
- `order-service/src/main/java/.../service/OrderServiceImpl.java` — **修改**：createOrder/getOrder/listOrders/cancelOrder 添加 `@SentinelResource` + fallback/blockHandler
- `evaluation-service/src/main/java/.../config/SentinelRuleConfig.java` — **新增**：流控规则（createEvaluation 10QPS、listEvaluations 30QPS）+ 异常比例熔断（≥30% 熔断 15s）
- `evaluation-service/src/main/java/.../impl/EvaluationServiceImpl.java` — **修改**：createEvaluation/approveEvaluation/rejectEvaluation/listEvaluations 添加 `@SentinelResource`

### 2. ✅ Ribbon 负载均衡多实例（已完成）
- `book-service/.../controller/BookController.java` — **修改**：新增 `/book/instance` 端点（返回端口号用于验证轮询）
- `book-service/src/main/resources/application-instance2.yml` — **新增**：第二实例配置
- `infra/start-multi-instances.bat` / `.sh` — **新增**：多实例启动脚本

### 3. ✅ 用户收货地址管理（已完成）
- `user-service/.../entity/Address.java` — **新增**：地址实体（12 字段）
- `user-service/.../repository/AddressRepository.java` — **新增**：MyBatis Plus Mapper
- `user-service/.../service/AddressService.java` / `AddressServiceImpl.java` — **新增**：完整 CRUD + 默认地址管理
- `user-service/.../controller/AddressController.java` — **新增**：6 个 REST 端点
- `infra/init.sql` — **修改**：新增 address 表 + 示例数据
- `frontend/index.html` — **修改**：新增地址管理 UI

### 4. 其他改动
- `README.md` — **修改**：更新多实例启动说明、Sentinel 规则表、地址管理 API
- `pom.xml` 及各模块 — 所有服务均编译打包成功

## 启动过程中的问题

### 基础设施 — 正常
- Nacos 2.4.3: ✅ 已启动（8848）
- Sentinel Dashboard 1.8.8: ✅ 已启动（8085）
- Zipkin Server: ✅ 已启动（9411）
- 5 个微服务: ✅ 均成功编译、打包、启动、注册到 Nacos

### MySQL — 有障碍 ⚠️

#### 背景
项目启动初期 MySQL 3306 端口上有 Windows Service 的 MySQL 8.0.44 在运行。此时微服务可以启动（Spring Boot 启动时不报错），但业务 API 调用时报 `Access denied for user 'root'@'localhost' (using password: NO)`，因为 root 用户的 `caching_sha2_password` 认证插件与空密码在 Windows 上存在兼容性问题。

#### 已尝试的解决方式

| 方法 | 结果 | 说明 |
|------|------|------|
| `mysql.exe -u root --password=` | ❌ | 始终 `(using password: NO)` — bash 环境传递参数有问题 |
| `MYSQL_PWD="" mysql.exe` | ❌ | 同上 |
| `--default-auth=mysql_native_password` | ❌ | 服务端要求 `caching_sha2_password` |
| `--get-server-public-key` | ❌ | 仍 `(using password: NO)` |
| Raw TCP 协议 `mysql_native_password` 认证 | ❌ | SHA1 计算正确，但服务端返回 `Access denied` |
| Raw TCP 协议 `caching_sha2_password` 认证 | ❌ | 服务端返回 auth switch 到 `mysql_native_password` |
| 用 `--skip-grant-tables` 启动 | ❌ | mysqld 启动后立即崩溃（InnoDB crash/abort） |
| `--innodb-force-recovery=1` | ❌ | 同样崩溃 |
| 用 Windows Service 的 my.ini (`--defaults-file`) | ❌ | 同样崩溃 |
| 重新 `--initialize-insecure` 新数据目录 | ❌ | 初始化成功，但启动仍崩溃 |
| `--default-authentication-plugin=mysql_native_password` | ❌ | 不影响 root 用户，root 仍用 `caching_sha2_password` |

#### 推测原因
- **Windows 11 Insider 预览版 (Build 26200)** 与 MySQL 8.0.44 存在兼容性问题，mysqld.exe 在启动 InnoDB 时异常崩溃
- 或 mysqld.exe 被安全软件/防病毒拦截
- bash 环境（Git Bash）缺少 `cmd.exe`、`taskkill`、`netstat` 等系统工具，难以管理 Windows 服务

#### 下次继续时的建议恢复步骤

1. **手动启动 MySQL**（在 Windows CMD 或 PowerShell 中）：
   ```cmd
   "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld" --datadir="C:\Users\31235\mysql-data" --port=3306
   ```

2. **或者启动 Windows Service MySQL**（如果已安装）：
   ```cmd
   net start MySQL80
   ```

3. **然后在 Claude Code 中继续启动微服务**：
   ```bash
   export JAVA_HOME="/c/Users/31235/.jdks/openjdk-23.0.1"
   cd "D:/Code/projects/bookstore"

   # 按顺序启动
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar user-service/target/user-service-1.0.0.jar &
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar book-service/target/book-service-1.0.0.jar --server.port=8081 &
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar book-service/target/book-service-1.0.0.jar --server.port=8086 &
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar order-service/target/order-service-1.0.0.jar &
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar evaluation-service/target/evaluation-service-1.0.0.jar &
   java --add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED -jar gateway-service/target/gateway-service-1.0.0.jar &
   ```

4. **如果 MySQL root 密码不是空**，需要修改各服务的 `application.yml` 中 `spring.datasource.password` 配置。

## MySQL 解决方案
原有的 Windows 服务 MySQL 8.0.44（3306）root 密码未知且无法绕过（`caching_sha2_password`认证，`--skip-grant-tables` 在 Windows 11 Insider Build 26200 上因 InnoDB 断言失败崩溃）。

**解决方案**：在同一机器上启动独立的 MySQL 实例：

```bash
# 1. 初始化新数据目录（成功）
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld.exe" \
  --initialize-insecure --datadir="C:\Users\31235\mysql-bookstore-data"

# 2. 在 3307 端口启动（需 --enable-named-pipe 否则 Windows 上 crash）
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld.exe" \
  --datadir="C:\Users\31235\mysql-bookstore-data" --port=3307 --enable-named-pipe

# 3. 初始化数据库
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -h 127.0.0.1 -P 3307 \
  --default-character-set=utf8mb4 < infra/init.sql

# 4. 修改各 service application.yml 中 jdbc 端口 3306→3307
```

**要点**：`--skip-grant-tables` 导致 Windows 上 crash；`--enable-named-pipe` 必须带上否则 mysqld 拒绝启动。

## 最终验证结果（2026-06-13 22:15）

### 基础设施
| 服务 | 端口 | 状态 |
|------|:----:|:----:|
| Nacos 注册中心 | 8848 | ✅ 运行中 |
| Sentinel Dashboard | 8085 | ✅ 运行中 |
| Zipkin 链路追踪 | 9411 | ✅ 运行中 |
| MySQL（新实例） | 3307 | ✅ 新建并初始化完成（root/空密码） |
| MySQL（原 Windows 服务） | 3306 | ✅ 仍在运行（保留不动） |

### 微服务（6个实例全部启动成功）
| 服务 | 端口 | 状态 |
|------|:----:|:----:|
| gateway-service | 8080 | ✅ UP，路由转发 + 鉴权 + CORS + 限流 |
| book-service-1 | 8081 | ✅ UP，已注册 Nacos |
| book-service-2 | 8086 | ✅ UP，已注册 Nacos（负载均衡验证通过） |
| user-service | 8082 | ✅ UP |
| order-service | 8083 | ✅ UP（含 Sentinel @SentinelResource） |
| evaluation-service | 8084 | ✅ UP（含 Sentinel @SentinelResource） |

### 功能验证通过
- ✅ 图书列表 API（直连 book-service + 网关路由 gateway）
- ✅ Ribbon 负载均衡（4次请求 → 端口 8086→8081→8086→8081 轮询）
- ✅ 用户登录（admin/123456 form POST）
- ✅ 订单创建（POST order-service，返回订单号及 PENDING 状态）
- ✅ 评价列表（bookId=1 返回2条 APPROVED 评价）
- ✅ 地址管理（userId=1 返回默认地址）
- ✅ 网关鉴权（白名单放行 `/api/book/`；非白名单返回 401 需要 Authorization 头）
- ✅ Sentinel 流控规则已配置（order-service 10/20/5 QPS；evaluation-service 10/30 QPS）
- ✅ Sentinel 熔断规则已配置（order-service RT>2000ms 慢调用熔断；evaluation-service 异常比例）
- ✅ Zipkin 链路追踪已集成（management.tracing.sampling.probability=1.0）
- ✅ Spring Cloud Stream + RabbitMQ 配置就绪（order-service → evaluation-service 消息驱动）

### 待优化
- 网关鉴权为简化实现，仅校验 Authorization 头是否存在，未做 JWT 解析
- 未做高并发实际验证 Sentinel 限流效果
- RabbitMQ 需要额外启动（当前未运行），因此消息驱动功能未验证

## 已确认可用的工具
- ✅ JDK 23.0.1
- ✅ Maven 3.9.8（通过 Java 直接调用，mvn 脚本不可用）
- ✅ Node.js v24.14.0
- ✅ Nacos 2.4.3
- ✅ Sentinel 1.8.8
- ✅ Zipkin
- ✅ MySQL Connector/J（应用中可用）
- ✅ MySQL Server 8.0.44（需 `--enable-named-pipe` 参数启动）
- ❌ cmd.exe / PowerShell / netstat / taskkill（Git Bash 中不可用）
- ❌ npm（缺少 bash）
